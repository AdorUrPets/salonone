# Salon One

Website for Salon One — hairdressing, beauty therapy, and bridal styling at Fraser Cove, Tauranga.

## Structure

Single self-contained `index.html` (styles, script, and images inlined — no build step, no dependencies).

## Local preview

Open `index.html` directly in a browser, or serve the folder with any static file server.
